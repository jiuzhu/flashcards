<ul>
  <% @students.each do |person| %>
  

  <li>    
    <img width='200' src='<%= person["image_url"]%>'>
    <h3>    
      <%= person["first_name"] %>
      <%= person["last_name"] %>
    <h3>
  </li>
</ul>

  <% end %>
